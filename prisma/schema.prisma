datasource db { provider = "sqlite"; url = "file:./dev.db" }
generator client { provider = "prisma-client-js" }

model Flow {
  id           String @id @default(cuid())
  userId       String?
  contentHash  String  // walrus blobId or sha256 in dev-nochain
  createdAt    DateTime @default(now())
}
